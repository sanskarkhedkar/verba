{
  "name": "verba-functions",
  "description": "Firebase backend for Verba AI and translation services",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-functions": "^6.0.0"
  }
}
